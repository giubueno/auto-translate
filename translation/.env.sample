# =============================================================================
# Translation Project - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.sample .env
# =============================================================================

# -----------------------------------------------------------------------------
# Translation Backend Selection
# -----------------------------------------------------------------------------
# Choose which service to use for translation.
# Options: "lmstudio", "openai", "gemini"
#
# If not set, auto-detects based on available API keys:
#   1. OPENAI_API_KEY present -> openai
#   2. GOOGLE_GEMINI_API_KEY present -> gemini
#   3. Default -> lmstudio (local, no API key needed)

# TRANSLATION_BACKEND=lmstudio

# -----------------------------------------------------------------------------
# LM Studio Configuration (local translation)
# -----------------------------------------------------------------------------
# LM Studio provides a local OpenAI-compatible API for running LLMs locally.
# Download from: https://lmstudio.ai/
# No API key required - runs entirely on your machine.

# Base URL for LM Studio API (default: http://localhost:1234/v1)
LMSTUDIO_BASE_URL=http://localhost:1234/v1

# Model to use for translation (default: qwen/qwen3-vl-8b:2)
LMSTUDIO_MODEL=qwen/qwen3-vl-8b:2

# -----------------------------------------------------------------------------
# OpenAI Configuration (for translation and text-to-speech)
# -----------------------------------------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys

OPENAI_API_KEY=your_openai_api_key_here

# Model to use for OpenAI translation (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# -----------------------------------------------------------------------------
# Google Cloud Configuration (optional)
# -----------------------------------------------------------------------------
# For Google Cloud Text-to-Speech and Translate services.
# Set up at: https://console.cloud.google.com/

# Path to your Google Cloud service account JSON credentials file
GOOGLE_APPLICATION_CREDENTIALS=path/to/your/google-credentials.json

# -----------------------------------------------------------------------------
# Google Gemini Configuration (for translation)
# -----------------------------------------------------------------------------
# For translation using Gemini.
# Get your API key from: https://aistudio.google.com/app/apikey

GOOGLE_GEMINI_API_KEY=your_gemini_api_key_here

# Model to use for Gemini translation (default: gemini-2.0-flash)
# GEMINI_MODEL=gemini-2.0-flash

# -----------------------------------------------------------------------------
# Testing Configuration (optional)
# -----------------------------------------------------------------------------
# Set to 1 to run integration tests that require LM Studio to be running

RUN_INTEGRATION_TESTS=0
